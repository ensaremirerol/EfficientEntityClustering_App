part of '../dashboard.dart';

class _DashboardSmall extends ConsumerWidget {
  const _DashboardSmall({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return Container(
      child: Center(
        child: Text('Home Page Small'),
      ),
    );
  }
}